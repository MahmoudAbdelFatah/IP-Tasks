function [ length ] = getLengthOfMaxBarValue(image)
%UNTITLED16 Summary of this function goes here
%   Detailed explanation goes here
  MaxX = getBottomLinePosition(image);
  MinX = getUpperLinePosition(image);
   length = MaxX - MinX ;
  figure, imshow(insertMarker(insertMarker(objectImage,[MinX , ]),[MinX ,MinY]));
end

